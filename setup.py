from setuptools import setup, find_packages
setup(
    name = 'core',
    packages = find_packages(),
)
setup(
    name = 'miner',
    packages = find_packages(),
)
setup(
    name = 'refiner',
    packages = find_packages(),
)
setup(
    name = 'exporter',
    packages = find_packages(),
)
setup(
    name = 'topology',
    packages = find_packages(),
)
